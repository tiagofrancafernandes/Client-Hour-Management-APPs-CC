# F2Apps Docker Compose - Development Environment
# ==============================================
#
# Usage:
#   1. Copy this file to docker-compose.yaml
#   2. Copy .env.docker.example to .env.docker
#   3. Configure your environment variables
#   4. Run: docker compose up -d
#
# For production, use docker-compose.prod.example.yaml
#
# Domains:
#   - local.hourledger.local.com       -> Landing Page (Nuxt 4)
#   - app.hourledger.local.com   -> Customer Frontend (Vue 3)
#   - admin.hourledger.local.com -> Backoffice (Nuxt 4)
#   - api.hourledger.local.com   -> API (Laravel)

name: f2apps

services:
    # ===========================================
    # Infrastructure Services
    # ===========================================

    # Nginx Reverse Proxy
    nginx:
        image: nginx:alpine
        container_name: f2apps-nginx
        restart: unless-stopped
        ports:
            - '${NGINX_HTTP_PORT:-80}:80'
            - '${NGINX_HTTPS_PORT:-443}:443'
            - '${BACKEND_PORT:-8000}:8000'
        volumes:
            - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
            - ./docker/nginx/api.conf:/etc/nginx/conf.d/api.conf:ro
            - ./docker/nginx/landingpage.conf:/etc/nginx/conf.d/landingpage.conf:ro
            - ./docker/nginx/frontend.conf:/etc/nginx/conf.d/frontend.conf:ro
            - ./docker/nginx/backoffice.conf:/etc/nginx/conf.d/backoffice.conf:ro
            - ./backend:/var/www/html/backend:ro
            - nginx-logs:/var/log/nginx
            # Uncomment to use custom hosts file
            # - ./docker/infra/etc-hosts:/etc/hosts:ro
        depends_on:
            - backend
            - frontend
            # - landingpage
            # - backoffice
        profiles:
            - default
            - backend
            - server
        networks:
            f2apps-network:
                aliases:
                    - local.hourledger.local.com
                    - app.hourledger.local.com
                    - admin.hourledger.local.com
                    - api.hourledger.local.com
        extra_hosts:
            - 'host.docker.internal:host-gateway'

    # PostgreSQL Database
    postgres:
        image: postgres:16-alpine
        container_name: f2apps-postgres
        restart: unless-stopped
        ports:
            - '${POSTGRES_PORT:-5432}:5432'
        environment:
            POSTGRES_USER: ${POSTGRES_USER:-f2apps}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-f2apps_secret}
            POSTGRES_DB: ${POSTGRES_DB:-f2apps}
            PGDATA: /var/lib/postgresql/data/pgdata
        volumes:
            - postgres-data:/var/lib/postgresql/data
            - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
        networks:
            - f2apps-network
        profiles:
            - default
            - backend
            - dbs
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-f2apps} -d ${POSTGRES_DB:-f2apps}']
            interval: 10s
            timeout: 5s
            retries: 5

    # Redis Cache
    redis:
        image: redis:7-alpine
        container_name: f2apps-redis
        restart: unless-stopped
        ports:
            - '${REDIS_PORT:-6379}:6379'
        command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-f2apps_redis}
        volumes:
            - redis-data:/data
        profiles:
            - default
            - backend
            - dbs
        networks:
            - f2apps-network
        healthcheck:
            test: ['CMD', 'redis-cli', '-a', '${REDIS_PASSWORD:-f2apps_redis}', 'ping']
            interval: 10s
            timeout: 5s
            retries: 5

    # ===========================================
    # Application Services
    # ===========================================

    # Laravel Backend API
    backend:
        env_file: ${DOCKER_ENV_FILE:-.env.docker}
        build:
            context: .
            dockerfile: ./docker/laravel/Dockerfile.dev
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: f2apps-backend
        restart: unless-stopped
        working_dir: /var/www/html/backend
        volumes:
            - ./backend:/var/www/html/backend
            - php-logs:/var/log/php
            - ./docker/log/xdebug.log:/var/log/php/xdebug.log:rw
            # Uncomment to use custom hosts file
            # - ./docker/infra/etc-hosts:/etc/hosts:ro
        # environment:
        #     APP_ENV: ${APP_ENV:-local}
        depends_on:
            postgres:
                condition: service_healthy
            redis:
                condition: service_healthy
        networks:
            - f2apps-network
        profiles:
            - default
            - backend
        extra_hosts:
            - 'host.docker.internal:host-gateway'

    # Vue 3 Customer Frontend
    frontend:
        env_file: ${DOCKER_ENV_FILE:-.env.docker}
        build:
            context: .
            dockerfile: ./docker/node/Dockerfile.vue.dev
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: f2apps-frontend
        restart: unless-stopped
        working_dir: /app
        ports:
            - '${FRONTEND_PORT:-5173}:5173'
        volumes:
            - ./frontend:/app
            - frontend-node-modules:/app/node_modules
            # Uncomment to use custom hosts file
            # - ./docker/infra/etc-hosts:/etc/hosts:ro
        environment:
            NODE_ENV: development
            VITE_API_URL: ${VITE_API_URL:-http://api.hourledger.local.com}
            VITE_APP_URL: ${VITE_APP_URL:-http://app.hourledger.local.com}
            # Domain environment variables
            VITE_SAAS_DOMAIN: ${VITE_SAAS_DOMAIN:-local.hourledger.local.com}
            VITE_CUSTOMER_APP_DOMAIN: ${VITE_CUSTOMER_APP_DOMAIN:-app.hourledger.local.com}
            VITE_BACKOFFICE_DOMAIN: ${VITE_BACKOFFICE_DOMAIN:-admin.hourledger.local.com}
            VITE_API_DOMAIN: ${VITE_API_DOMAIN:-api.hourledger.local.com}
        # command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
        command: npm run dev -- --host
        networks:
            - f2apps-network
        extra_hosts:
            - 'host.docker.internal:host-gateway'

    # Nuxt 4 Landing Page
    landingpage:
        env_file: ${DOCKER_ENV_FILE:-.env.docker}
        build:
            context: .
            dockerfile: ./docker/node/Dockerfile.nuxt.dev
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: f2apps-landingpage
        restart: unless-stopped
        working_dir: /app
        ports:
            - '${LANDINGPAGE_PORT:-3000}:3000'
        volumes:
            - ./landingpage:/app
            - landingpage-node-modules:/app/node_modules
            # Uncomment to use custom hosts file
            # - ./docker/infra/etc-hosts:/etc/hosts:ro
        environment:
            NODE_ENV: development
            NUXT_PUBLIC_API_URL: http://api.hourledger.local.com
            NUXT_PUBLIC_APP_URL: http://local.hourledger.local.com
            # Domain environment variables
            NUXT_PUBLIC_SAAS_DOMAIN: local.hourledger.local.com
            NUXT_PUBLIC_CUSTOMER_APP_DOMAIN: app.hourledger.local.com
            NUXT_PUBLIC_BACKOFFICE_DOMAIN: admin.hourledger.local.com
            NUXT_PUBLIC_API_DOMAIN: api.hourledger.local.com
        command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
        profiles:
            - front
            - other
        networks:
            - f2apps-network
        extra_hosts:
            - 'host.docker.internal:host-gateway'

    # Nuxt 4 Backoffice
    backoffice:
        env_file: ${DOCKER_ENV_FILE:-.env.docker}
        build:
            context: .
            dockerfile: ./docker/node/Dockerfile.nuxt.dev
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: f2apps-backoffice
        restart: unless-stopped
        working_dir: /app
        ports:
            - '${BACKOFFICE_PORT:-3001}:3000'
        volumes:
            - ./frontend-backoffice:/app
            - backoffice-node-modules:/app/node_modules
            # Uncomment to use custom hosts file
            # - ./docker/infra/etc-hosts:/etc/hosts:ro
        environment:
            NODE_ENV: development
            NUXT_PUBLIC_API_URL: http://api.hourledger.local.com
            NUXT_PUBLIC_APP_URL: http://admin.hourledger.local.com
            # Domain environment variables
            NUXT_PUBLIC_SAAS_DOMAIN: local.hourledger.local.com
            NUXT_PUBLIC_CUSTOMER_APP_DOMAIN: app.hourledger.local.com
            NUXT_PUBLIC_BACKOFFICE_DOMAIN: admin.hourledger.local.com
            NUXT_PUBLIC_API_DOMAIN: api.hourledger.local.com
        command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
        profiles:
            - front
            - other
        networks:
            - f2apps-network
        extra_hosts:
            - 'host.docker.internal:host-gateway'

    # ===========================================
    # Development Tools (Optional)
    # ===========================================

    # Laravel Queue Worker
    queue-worker:
        env_file: ${DOCKER_ENV_FILE:-.env.docker}
        build:
            context: .
            dockerfile: ./docker/laravel/Dockerfile.dev
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: f2apps-queue-worker
        restart: unless-stopped
        working_dir: /var/www/html/backend
        volumes:
            - ./backend:/var/www/html/backend
        environment:
            APP_ENV: ${APP_ENV:-local}
            DB_CONNECTION: pgsql
            DB_HOST: postgres
            DB_PORT: 5432
            DB_DATABASE: ${POSTGRES_DB:-f2apps}
            DB_USERNAME: ${POSTGRES_USER:-f2apps}
            DB_PASSWORD: ${POSTGRES_PASSWORD:-f2apps_secret}
            REDIS_HOST: redis
            REDIS_PASSWORD: ${REDIS_PASSWORD:-f2apps_redis}
            REDIS_PORT: 6379
        command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
        depends_on:
            - backend
            - redis
        networks:
            - f2apps-network
        profiles:
            - workers

    # Laravel Scheduler
    scheduler:
        env_file: ${DOCKER_ENV_FILE:-.env.docker}
        build:
            context: .
            dockerfile: ./docker/laravel/Dockerfile.dev
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: f2apps-scheduler
        restart: unless-stopped
        working_dir: /var/www/html/backend
        volumes:
            - ./backend:/var/www/html/backend
        environment:
            APP_ENV: ${APP_ENV:-local}
            DB_CONNECTION: pgsql
            DB_HOST: postgres
            DB_PORT: 5432
            DB_DATABASE: ${POSTGRES_DB:-f2apps}
            DB_USERNAME: ${POSTGRES_USER:-f2apps}
            DB_PASSWORD: ${POSTGRES_PASSWORD:-f2apps_secret}
            REDIS_HOST: redis
            REDIS_PASSWORD: ${REDIS_PASSWORD:-f2apps_redis}
            REDIS_PORT: 6379
        command: sh -c "while true; do php artisan schedule:run --verbose --no-interaction; sleep 60; done"
        depends_on:
            - backend
        networks:
            - f2apps-network
        profiles:
            - workers

# ===========================================
# Networks
# ===========================================
networks:
    f2apps-network:
        driver: bridge
        ipam:
            config:
                - subnet: 172.28.0.0/16

# ===========================================
# Volumes
# ===========================================
volumes:
    postgres-data:
        driver: local
    redis-data:
        driver: local
    nginx-logs:
        driver: local
    php-logs:
        driver: local
    frontend-node-modules:
        driver: local
    landingpage-node-modules:
        driver: local
    backoffice-node-modules:
        driver: local
